name: Windows Build

# Windows-only build: zip bundle + setup.exe installer, no other platforms,
# no quality gate, no GitHub Release publishing.

on:
  workflow_dispatch:
  push:
    branches:
      - 'windows**'

permissions:
  contents: read

concurrency:
  group: mithka-windows-build-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: '3.44.2'

jobs:
  build-windows:
    name: Build Windows x64
    runs-on: windows-2022
    timeout-minutes: 150
    env:
      # MDK/FFmpeg native SDK for the video stack: fvp's cmake downloads an
      # archive pinned by sha256 (x64-windows-vs2026.7z) from the upstream
      # author's release. Point it at this fork's own mirror of that exact
      # archive — the sha256 below is the one fvp pins, so integrity proof is
      # unchanged while the build no longer depends on someone else's hosting.
      FVP_DEPS_URL: https://github.com/1050076/mithka-tdjson/releases/download/fvp-mdk-2026-08-14
      FVP_DEPS_SHA256: c541fa13dd12e17414eb05e6a176f3fb40a93cc5c38aaf85dbce1cd68d2828f0

    steps:
      - uses: actions/checkout@v7

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install tdjson build dependencies
        shell: pwsh
        run: |
          choco install -y gperf
          & "$env:VCPKG_INSTALLATION_ROOT\vcpkg.exe" install zlib:x64-windows-static openssl:x64-windows-static
          $zlib = & "$env:VCPKG_INSTALLATION_ROOT\vcpkg.exe" list zlib:x64-windows-static
          if (-not $zlib) { throw "zlib:x64-windows-static not installed" }

      - name: Build tdjson from verified source
        shell: bash
        run: |
          scripts/build-tdjson-from-source.sh windows native-libs/tdjson.dll x64

      - name: Resolve Flutter dependencies
        run: flutter pub get

      - name: Verify git dependency revisions
        shell: bash
        run: bash scripts/check-git-dep-revisions.sh

      - name: Write credential-free secrets.dart
        shell: bash
        run: |
          # No Telegram api id/hash is compiled in: this fork's artifacts are
          # public, so a real value here would be extractable from the binary.
          # The app prompts for the user's own credentials in Settings on first
          # run and uses those (lib/settings/api_credentials_config.dart).
          mkdir -p lib/config
          cat > lib/config/secrets.dart <<'EOF'
          class Secrets {
            static const int apiId = 0;
            static const String apiHash = 'not-configured';
            static bool get isConfigured => apiId != 0 && apiHash.isNotEmpty;
          }
          EOF

      - name: Build Windows release
        shell: pwsh
        run: |
          $stamp = Get-Date -AsUTC -Format 'yyMMddHH'
          $shortSha = "${{ github.sha }}".Substring(0, 7)
          flutter build windows --release --no-pub `
            --build-name="1.5.0" `
            --build-number="$stamp" `
            --dart-define="GIT_COMMIT=$shortSha" `
            --dart-define="CI_BUILD_STAMP=$stamp" `
            --dart-define="SENTRY_DSN="

      - name: Assert no build-time credentials in the binary
        shell: bash
        run: bash scripts/check-no-embedded-credentials.sh "build/windows/x64/runner/Release"

      - name: Package Windows release
        shell: pwsh
        run: |
          $bundle = "build\windows\x64\runner\Release"
          Copy-Item "native-libs\tdjson.dll" "$bundle\tdjson.dll"
          Add-Type -TypeDefinition @'
          using System;
          using System.Runtime.InteropServices;
          public static class NativeLibraryCheck {
            [DllImport("kernel32", SetLastError = true, CharSet = CharSet.Unicode)]
            public static extern IntPtr LoadLibrary(string path);
            [DllImport("kernel32", SetLastError = true)]
            public static extern IntPtr GetProcAddress(IntPtr module, string name);
            [DllImport("kernel32", SetLastError = true)]
            public static extern bool FreeLibrary(IntPtr module);
          }
          '@
          $module = [NativeLibraryCheck]::LoadLibrary((Resolve-Path "$bundle\tdjson.dll"))
          if ($module -eq [IntPtr]::Zero) {
            throw "Could not load packaged tdjson.dll: $([Runtime.InteropServices.Marshal]::GetLastWin32Error())"
          }
          $requiredSymbols = @(
            "td_create_client_id",
            "td_mithka_export_session_string",
            "td_mithka_import_session_string",
            "td_mithka_last_error",
            "td_mithka_set_transfer_boost"
          )
          foreach ($symbol in $requiredSymbols) {
            if ([NativeLibraryCheck]::GetProcAddress($module, $symbol) -eq [IntPtr]::Zero) {
              throw "Packaged tdjson.dll does not export $symbol"
            }
          }
          [void][NativeLibraryCheck]::FreeLibrary($module)
          New-Item -ItemType Directory -Force dist\package | Out-Null
          Copy-Item $bundle dist\package\Mithka -Recurse
          Compress-Archive -Path dist\package\Mithka `
            -DestinationPath "dist\mithka-windows-x64.zip"

          $installer = & scripts\build-windows-installer.ps1 `
            -Architecture "x64" `
            -BuildDirectory $bundle `
            -OutputDirectory dist `
            -Version "1.5.0" `
            -ArtifactLabel "windows-local"
          if (-not (Test-Path -LiteralPath $installer -PathType Leaf)) {
            throw "Windows installer was not created: $installer"
          }
          Get-ChildItem dist

      - name: Upload Windows zip artifact
        uses: actions/upload-artifact@v7
        with:
          name: mithka-windows-x64-zip
          path: dist/mithka-windows-x64.zip
          if-no-files-found: error
          retention-days: 1

      - name: Upload Windows installer artifact
        uses: actions/upload-artifact@v7
        with:
          name: mithka-windows-x64-setup
          path: dist/mithka-windows-local-windows-x64-setup.exe
          if-no-files-found: error
          retention-days: 1
